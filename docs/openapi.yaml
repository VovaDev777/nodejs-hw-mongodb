openapi: 3.1.0
info:
  version: 1.0.0
  title: My contacts app
  license:
    name: Apache 2.0
    url: http://www.apache.org/licenses/LICENSE-2.0.html
  description:
    Contacts app documentation
tags:
  - name: Contacts
    description: Operations about contacts.
  - name: Auth
    description: Operations about auth.
servers:
  - url: http://localhost:3000
  - url: https://nodejs-hw-mongodb-yfkk.onrender.com
paths:
   /contacts: 
      get:
        $ref: ../swagger/paths/contacts/get.yaml
      post: 
        $ref: ../swagger/paths/contacts/post.yaml
   /contacts/{contactId}:
      get: 
        $ref: ../swagger/paths/contacts/getById.yaml
      patch:
        $ref: ../swagger/paths/contacts/patch.yaml
      delete:
        $ref: ../swagger/paths/contacts/delete.yaml
components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
